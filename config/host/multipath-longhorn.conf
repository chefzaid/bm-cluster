# Longhorn presents one iSCSI path per volume. Device Mapper Multipath must not
# claim these devices, or kubelet cannot format/mount newly provisioned PVCs.
blacklist {
    device {
        vendor "IET"
        product "VIRTUAL-DISK"
    }
}
