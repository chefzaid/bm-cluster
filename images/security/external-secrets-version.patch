diff --git a/cmd/controller/root.go b/cmd/controller/root.go
index 5ff1742..ea981cb 100644
--- a/cmd/controller/root.go
+++ b/cmd/controller/root.go
@@ -129,10 +129,14 @@ func init() {
 	utilruntime.Must(genv1alpha1.AddToScheme(scheme))
 }
 
+// version is set from the verified upstream release by the security build.
+var version = "development"
+
 var rootCmd = &cobra.Command{
-	Use:   "external-secrets",
-	Short: "operator that reconciles ExternalSecrets and SecretStores",
-	Long:  `For more information visit https://external-secrets.io`,
+	Use:     "external-secrets",
+	Version: version,
+	Short:   "operator that reconciles ExternalSecrets and SecretStores",
+	Long:    `For more information visit https://external-secrets.io`,
 	Run: func(cmd *cobra.Command, _ []string) {
 		setupLogger()
 
