---
- name: Deploy Infrastructure to Kubernetes
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    infra_namespace: "{{ infra_ns | default('infra') }}"
    k8s_dir: "{{ playbook_dir }}/../deployments"
    server_exposure_mode: "{{ server_exposure | default('internet') | lower }}"
    apply_host_security_suite: "{{ harden_firewall | default(server_exposure_mode == 'internet') }}"
    trusted_node_cidr: "{{ k3s_node_network_cidr | default(lookup('env', 'K3S_NODE_NETWORK_CIDR'), true) }}"
    deploy_odoo: "{{ install_odoo | default(true) }}"
    ingress_nginx_chart_version: "4.15.1"
    argocd_chart_version: "9.5.11"
    argocd_image_tag: "v3.3.10"

  tasks:
    - name: Validate server exposure mode
      assert:
        that:
          - server_exposure_mode in ['internet', 'local']
        fail_msg: "server_exposure must be either 'internet' or 'local'."

    - name: Ensure namespaces exist
      shell: kubectl create namespace {{ item }} --dry-run=client -o yaml | kubectl apply -f -
      loop:
        - "{{ infra_namespace }}"

    - name: Apply Kubernetes admission and namespace security baseline
      command: kubectl apply -f {{ k8s_dir }}/security-baseline.yaml
      register: security_baseline
      changed_when: "'created' in security_baseline.stdout or 'configured' in security_baseline.stdout"

    - name: Configure the enforced K3s AppArmor runtime profile
      command: "{{ playbook_dir }}/../scripts/configure-k3s-apparmor.sh"

    - name: Configure daily consistent K3s recovery archives
      command: "{{ playbook_dir }}/../scripts/configure-k3s-backups.sh"

    - name: Apply internet-facing host security tooling
      command: "{{ playbook_dir }}/../scripts/configure-node-security.sh --apply --server-exposure {{ server_exposure_mode }}"
      environment:
        K3S_NODE_NETWORK_CIDR: "{{ trusted_node_cidr }}"
      when:
        - server_exposure_mode == 'internet'
        - apply_host_security_suite | bool

    - name: Publish host security policy record to Kubernetes
      command: kubectl apply -f {{ k8s_dir }}/host-security-config.yaml
      when: server_exposure_mode == 'internet'
      register: host_security_config
      changed_when: "'created' in host_security_config.stdout or 'configured' in host_security_config.stdout"

    - name: Skip internet-facing host security tooling for local-only servers
      debug:
        msg: "server_exposure=local, skipping UFW, Fail2ban, CrowdSec, and Lynis automation."
      when: server_exposure_mode == 'local'

    - name: Add Helm repositories
      shell: |
        helm repo add hashicorp https://helm.releases.hashicorp.com 2>/dev/null || true
        helm repo add external-secrets https://charts.external-secrets.io 2>/dev/null || true
        helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx 2>/dev/null || true
        helm repo update >/dev/null 2>&1
      changed_when: false

    - name: Install or upgrade NGINX Ingress
      command: >
        helm upgrade --install ingress-nginx ingress-nginx/ingress-nginx
        --namespace {{ infra_namespace }}
        --version {{ ingress_nginx_chart_version }}
        --set controller.service.type=LoadBalancer
        --set controller.service.enableHttp=true
        --wait --timeout 300s

    - name: Install or upgrade Argo CD on the security-patched release
      command: >
        helm upgrade --install argocd argo/argo-cd
        --namespace {{ infra_namespace }}
        --version {{ argocd_chart_version }}
        --set-string global.image.tag={{ argocd_image_tag }}
        --set server.service.type=ClusterIP
        --set configs.params.server\.insecure=true
        --set redis.enabled=true
        --wait --timeout 600s

    - name: Install or upgrade Vault
      command: >
        helm upgrade --install vault hashicorp/vault
        --namespace {{ infra_namespace }}
        --set injector.enabled=false
        --set server.ha.enabled=true
        --set server.ha.raft.enabled=true
        --set server.ha.replicas=1
        --set server.dataStorage.storageClass=longhorn

    - name: Install or upgrade External Secrets Operator
      command: >
        helm upgrade --install external-secrets external-secrets/external-secrets
        --namespace {{ infra_namespace }}
        --set installCRDs=true
        --wait --timeout 300s

    - name: Apply unified Vault manifests (ingress, RBAC, and secret sync)
      command: kubectl apply -f {{ k8s_dir }}/vault.yaml
      register: result
      changed_when: "'created' in result.stdout or 'configured' in result.stdout"

    - name: Wait for Vault pod phase running
      command: kubectl wait --for=jsonpath='{.status.phase}'=Running pod/vault-0 -n {{ infra_namespace }} --timeout=300s
      ignore_errors: yes

    - name: Wait for External Secrets controller
      command: kubectl wait --for=condition=ready pod -l app.kubernetes.io/name=external-secrets -n {{ infra_namespace }} --timeout=300s
      ignore_errors: yes

    - name: Bootstrap Vault auth, policies, and seed secrets
      command: "{{ playbook_dir }}/../scripts/configure-vault.sh {{ infra_namespace }}"

    - name: Wait for ExternalSecret reconciliation
      command: kubectl wait --for=condition=Ready externalsecret/{{ item }} -n {{ infra_namespace }} --timeout=180s
      loop:
        - postgres-secret
        - mongodb-secret
        - odoo-secret
        - sonarqube-db-credentials
        - grafana-admin-secret
        - keycloak-admin-secret
        - keycloak-realm-config
        - jenkins-maven-settings
        - jenkins-npm-config
        - dbgate-auth-secret
        - kafka-ui-auth-secret
        - portainer-auth-secret
      ignore_errors: yes

    - name: Deploy data stores
      command: kubectl apply -f {{ k8s_dir }}/{{ item }}
      loop:
        - postgres.yaml
        - kafka.yaml
        - redis.yaml
        - mongodb.yaml
      register: result
      changed_when: "'created' in result.stdout or 'configured' in result.stdout"

    - name: Wait for data stores
      command: kubectl wait --for=condition=ready pod -l app={{ item }} -n {{ infra_namespace }} --timeout=180s
      loop: [postgres, redis, mongodb, zookeeper, kafka]
      ignore_errors: yes

    - name: Deploy platform services
      command: kubectl apply -f {{ k8s_dir }}/{{ item }}
      loop:
        - keycloak.yaml
        - monitoring.yaml
        - elk.yaml
        - logging-agent.yaml
        - jenkins.yaml
        - sonarqube.yaml
        - nexus.yaml
        - gitlab.yaml
        - dbgate.yaml
        - kafka-ui.yaml
        - portainer.yaml
        - homepage.yaml
        - ingress.yaml
      register: result
      changed_when: "'created' in result.stdout or 'configured' in result.stdout"

    - name: Wait for Nexus before configuring the private registry
      command: kubectl rollout status deployment/nexus -n {{ infra_namespace }} --timeout=600s

    - name: Configure the private Nexus registry and least-privilege service accounts
      command: "{{ playbook_dir }}/../scripts/configure-nexus-registry.sh"

    - name: Install descheduler addon resources (manual run mode)
      command: kubectl apply -f {{ k8s_dir }}/descheduler.yaml
      register: result
      changed_when: "'created' in result.stdout or 'configured' in result.stdout"

    - name: Deploy Odoo
      command: kubectl apply -f {{ k8s_dir }}/{{ item }}
      loop:
        - odoo.yaml
        - ingress.yaml
      when: deploy_odoo | bool
      register: odoo_result
      changed_when: "'created' in odoo_result.stdout or 'configured' in odoo_result.stdout"

    - name: Wait for platform services
      command: kubectl wait --for=condition=ready pod -l app={{ item }} -n {{ infra_namespace }} --timeout=300s
      loop: [keycloak, jenkins, elasticsearch, kibana, gitlab, dbgate, kafka-ui, portainer, homepage]
      ignore_errors: yes

    - name: Wait for cluster observability DaemonSets
      command: kubectl rollout status daemonset/{{ item }} -n {{ infra_namespace }} --timeout=180s
      loop: [node-exporter, fluent-bit]
      ignore_errors: yes

    - name: Wait for Odoo
      command: kubectl wait --for=condition=ready pod -l app=odoo -n {{ infra_namespace }} --timeout=900s
      when: deploy_odoo | bool
      ignore_errors: yes

    - name: Display status
      command: kubectl get pods -n {{ infra_namespace }} --no-headers
      register: pods

    - debug:
        msg: "{{ pods.stdout_lines }}"
